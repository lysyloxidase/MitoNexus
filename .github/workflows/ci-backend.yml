name: CI Backend

on:
  push:
    paths:
      - "apps/backend/**"
      - ".github/workflows/ci-backend.yml"
  pull_request:
    paths:
      - "apps/backend/**"
      - ".github/workflows/ci-backend.yml"

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: pgvector/pgvector:pg17
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: test
        ports:
          - "5432:5432"
        options: >-
          --health-cmd pg_isready --health-interval 10s
          --health-timeout 5s --health-retries 5
      neo4j:
        image: neo4j:2025-community
        env:
          NEO4J_AUTH: neo4j/test
        ports:
          - "7474:7474"
          - "7687:7687"
        options: >-
          --health-cmd "cypher-shell -u neo4j -p test 'RETURN 1;'"
          --health-interval 10s --health-timeout 10s --health-retries 10
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Create virtual environment
        working-directory: apps/backend
        run: |
          uv venv .venv
          echo "$PWD/.venv/bin" >> "$GITHUB_PATH"

      - name: Install dependencies
        working-directory: apps/backend
        run: uv pip install -e ".[dev]"

      - name: Lint with ruff
        working-directory: apps/backend
        run: ruff check . && ruff format --check .

      - name: Type-check with mypy
        working-directory: apps/backend
        run: mypy src

      - name: Test with pytest
        working-directory: apps/backend
        env:
          MITONEXUS_DATABASE_URL: postgresql+asyncpg://test:test@localhost:5432/test
          MITONEXUS_REDIS_URL: redis://localhost:6379/0
          MITONEXUS_NEO4J_URI: bolt://localhost:7687
          MITONEXUS_NEO4J_USER: neo4j
          MITONEXUS_NEO4J_PASSWORD: test
        run: pytest --cov=src --cov-report=term

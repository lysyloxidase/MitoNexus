# Backend

FastAPI backend scaffold for MitoNexus. Phase 1 includes configuration management, a minimal app
entrypoint, Alembic wiring, tests, and container packaging.

## Quick Commands

```bash
uv pip install -e ".[dev]"
ruff check .
mypy src
pytest
```
